#include <stdio.h>

/**
 * main -Entry point
 * Return :Always 0
 */

int main(void)
{
  int n;
  int m;

  for (n = 97; nn <= 122; n++)
    {
      putchar(n);
    }
  for (m = 65; m<= 90; m++)
    {
      putchar(m);
    }
  putchar('\n');
  return (0);
}
